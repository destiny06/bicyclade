syntax = "proto3";

import "player_action.proto"
import "server_action.proto"
import "spectator_action.proto"

package bicyclade;

message Container {
  optional PlayerAction player_action;
  optional ServerAction server_action;
  optional SpectatorAction spectator_action;
}
